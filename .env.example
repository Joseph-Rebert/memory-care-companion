# Copy to .env and fill in your keys. .env is gitignored — never commit real keys.

# Chat model (the caregiver chatbot):
ANTHROPIC_API_KEY=

# Embeddings for RAG retrieval:
VOYAGE_API_KEY=

# Optional — only raise rate limits for paper searches:
NCBI_API_KEY=
S2_API_KEY=
CONTACT_EMAIL=
